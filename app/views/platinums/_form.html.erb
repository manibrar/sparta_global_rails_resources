<%= form_with(model: platinum, local: true) do |form| %>
  <% if platinum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(platinum.errors.count, "error") %> prohibited this platinum from being saved:</h2>

      <ul>
      <% platinum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
