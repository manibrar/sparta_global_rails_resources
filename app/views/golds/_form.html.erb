<%= form_with(model: gold, local: true) do |form| %>
  <% if gold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gold.errors.count, "error") %> prohibited this gold from being saved:</h2>

      <ul>
      <% gold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
